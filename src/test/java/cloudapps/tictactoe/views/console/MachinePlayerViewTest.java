package cloudapps.tictactoe.views.console;
/*
import static org.junit.Assert.assertNotNull;

import org.junit.Test;

import cloudapps.tictactoe.controllers.PlayController;
import cloudapps.tictactoe.models.Coordinate;
import cloudapps.tictactoe.models.Game;

class MachinePlayerViewTest {

    private Game game;

    private MachinePlayerView machinePlayerView;

    MachinePlayerViewTest() {
        this.game = new Game();
        this.game.setUsers(0);
        this.machinePlayerView = new MachinePlayerView(new PlayController(this.game));
    }

    @Test
    void testGivenNewCoordinateViewWhenReadCoordinateThenIsCorrect() {
        Coordinate coordinate = this.machinePlayerView.readCoordinateToPut();
        assertNotNull(coordinate);
    }

    @Test
    void testGivenNewCoordinateViewWhenReadCoordinateThenIsCorrec() {
        Coordinate coordinate1 = this.machinePlayerView.readCoordinateToPut();
        this.game.putTokenPlayerFromTurn(coordinate1);
        Coordinate coordinate2 = this.machinePlayerView.readCoordinateToPut();
        this.game.putTokenPlayerFromTurn(coordinate2);
        Coordinate[] coordinates = this.machinePlayerView.readCoordinatesToMove();
        assertNotNull(coordinates[0]);
        assertNotNull(coordinates[1]);
    }

}
*/
